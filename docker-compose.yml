services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: capytal
      POSTGRES_PASSWORD: capytal
      POSTGRES_DB: capytal
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
